<%- include('../../components/headerpub.ejs') %>

<div class="container py-5">
  <h1 class="mb-4">Hamburguesas de <%= restaurante.nombre %></h1>

  <div class="row">
    <% restaurante.hamburguesas.forEach(hamburguesa => { %>
      <div class="col-md-4 mb-2">
        <div class="card h-100">
          <img src="<%= hamburguesa.foto %>" class="card-img-top" height="210" alt="<%= hamburguesa.nombre %>">
          <div class="card-body d-inline-flex flex-column justify-content-between">
            <h5 class="card-title text-center"><%= hamburguesa.nombre %></h5>
            <a href="/hamburguesa/<%= hamburguesa.id %>" class="btn btn-outline-success my-2 my-sm-0">Ver detalles</a>
          </div>
        </div>
      </div>
    <% }); %>
  </div>
</div>

<%- include('../../components/footer.ejs') %>
